# Settings
config/settings.json

# Real manifests (examples are committed)
manifests/*.json
!manifests/*.example.json

# Local workspace artifacts (ONLY if you choose to create them inside this repo)
# Note: .gitignore cannot ignore sibling folders outside the repo.
_cache/
_source/
CIS_GAME/

# OS
.DS_Store
Thumbs.db
